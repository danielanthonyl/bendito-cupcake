import { StyleSheet } from 'react-native';

export const CartSuggestionListStyles = StyleSheet.create({
  wrapper: {
    gap: 16,
    marginBottom: 8,
    marginHorizontal: 20,
  },

  title: {
    marginLeft: 18,
  },
  container: {
    gap: 14,
    flexDirection: 'row',
    marginRight: 10,
  },
});
