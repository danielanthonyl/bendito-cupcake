import { StyleSheet } from 'react-native';

export const CartItemListStyles = StyleSheet.create({
  wrapper: {
    gap: 8,
    margin: 20,
  },
  empty: {
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 14,
    padding: 12,
  },
});
