import { StyleSheet } from 'react-native';

export const ProductIngredientsStyles = StyleSheet.create({
  table: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 4,
  },
  name: { color: 'black' },
});
