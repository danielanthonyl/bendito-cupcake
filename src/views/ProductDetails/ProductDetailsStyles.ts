import { StyleSheet } from 'react-native';

export const ProductDetailsStyles = StyleSheet.create({
  container: {
    paddingVertical: 12,
    paddingHorizontal: 8,
    gap: 12,
  },
  title: {
    color: 'black',
    marginBottom: 8,
  },
});
