import { StyleSheet } from 'react-native';

export const ShopDetailsStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  textImageContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  name: { color: 'black' },
  ratingContainer: { flexDirection: 'row',
  justifyContent: 'center',
},
  ratingTextContainer: { alignItems: 'center' },
});
