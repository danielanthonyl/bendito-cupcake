import { StyleSheet } from 'react-native';
import { Colors } from '../../../constants/colors';

export const OrderOptionsStyles = StyleSheet.create({
  wrapper: {
    gap: 16,
    padding: 18,
    marginBottom: 80,
  },
  container: {
    padding: 16,
    backgroundColor: Colors.softBlue,
    borderRadius: 100,
    alignItems: 'center',
  },
});
