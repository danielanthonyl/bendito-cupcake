import { StyleSheet } from 'react-native';

export const OrderTrackStyles = StyleSheet.create({
  wrapper: { padding: 12, gap: 10 },
  image: {
    width: undefined,
    height: 'auto',
    aspectRatio: 1.39,
  },
  imageFooter: { flexDirection: 'row', padding: 10 },
});
