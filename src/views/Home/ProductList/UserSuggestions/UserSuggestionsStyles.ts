import { StyleSheet } from 'react-native';

export const UserSuggestionStyles = StyleSheet.create({
  listContainer: {
    flexWrap: 'wrap',
    flexDirection: 'row',
    gap: 20,
    marginBottom: 20,
    justifyContent: 'space-between',
    width: '100%',
  },
  title: {
    marginBottom: 10,
  },
});
