import { StyleSheet } from 'react-native';

export const FilterIconsStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: 40,
  },
  itemContainer: {
    flexDirection: 'column',
    alignItems: 'center',
    gap: 10,
  },
});
